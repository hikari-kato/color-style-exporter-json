{
  "name": "color-style-exporter-json",
  "version": "1.0.0",
  "description": "Export Figma color styles to JSON",
  "main": "code.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json -w"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.42.1",
    "typescript": "^4.5.5"
  }
}